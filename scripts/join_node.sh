#!/bin/sh

#Run command "kubeadm token create --print-join-command" on master node and create a script called "join.sh" with the output. Run this scipt on the worker nodes to join them with master node.
