apiVersion: v1
kind: Service
metadata:
  name: svc
  labels:
    name: nginx
spec:
  selector:
    name: nginx
  type: NodePort
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
